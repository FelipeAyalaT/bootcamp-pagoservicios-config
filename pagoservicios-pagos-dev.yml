server:
  port: 8088

spring:
  application:
    name: pagoservicios-pagos
  config:
    import: optional:configserver:http://${HOST_API_CONFIGSERVER}:8888
  r2dbc:
    url: r2dbc:pool:mysql//${HOST_MYSQL_SERVER}:1433/BDPAGOSERVICIOS?useUnicode=true&characterEncoding=UTF-8&useSSL=false&useLocalSessionState=true
    username: root
    password: toor
logging:
  level:
    org.springframework.data.r2dbc: DEBUG